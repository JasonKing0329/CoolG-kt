<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">
        <com.king.app.coolg_kt.view.widget.rc.RCImageView
            android:id="@+id/iv_star"
            android:layout_width="@dimen/pad_record_star_size"
            android:layout_height="@dimen/pad_record_star_size"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_def_person"
            app:round_corner="@dimen/pad_record_star_radius"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="120dp"
            android:gravity="right"
            android:orientation="vertical"
            android:layout_marginLeft="15dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/iv_star">
            <TextView
                android:id="@+id/tv_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#dfdfdf"
                android:textSize="18dp"
                android:text="Robbie Rojo" />
            <TextView
                android:id="@+id/tv_flag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:textSize="14dp"
                android:textColor="#dfdfdf"
                android:text="top(98/24)" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
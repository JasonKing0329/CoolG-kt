<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="bean"
            type="com.king.app.coolg_kt.page.match.ScoreHead" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_period_rank"
            style="@style/TvTagPeriod"
            android:text="Rank Period"
            android:layout_marginStart="16dp"
            android:layout_marginTop="10dp"
            app:selected="@{bean.selectedType == 0}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView
            android:id="@+id/tv_period_rtf"
            style="@style/TvTagPeriod"
            android:text="Race to Final"
            android:layout_marginStart="10dp"
            app:selected="@{bean.selectedType == 1}"
            app:layout_constraintStart_toEndOf="@+id/tv_period_rank"
            app:layout_constraintTop_toTopOf="@+id/tv_period_rank"/>

        <TextView
            android:id="@+id/tv_period_specific"
            style="@style/TvTagPeriod"
            android:text="@{bean.periodSpecificText}"
            android:layout_marginStart="10dp"
            app:selected="@{bean.selectedType == 2}"
            app:layout_constraintStart_toEndOf="@+id/tv_period_rtf"
            app:layout_constraintTop_toTopOf="@+id/tv_period_rank"/>

        <TextView
            android:id="@+id/tv_total"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="16dp"
            android:layout_marginTop="4dp"
            android:text="@{bean.scoreText}"
            app:layout_constraintTop_toBottomOf="@+id/tv_period_rank"
            app:layout_constraintStart_toStartOf="@+id/tv_period_rank"
            tools:text="Total: 10800"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
<?xml version="1.0" encoding="utf-8"?>
<layout>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/tv_name"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/records_tag_height"
            android:paddingLeft="@dimen/records_tag_padding"
            android:paddingRight="@dimen/records_tag_padding"
            android:textColor="@drawable/selector_studio_tag_text"
            android:background="@drawable/selector_studio_tag_bg"
            android:textSize="@dimen/records_tag_text"
            android:gravity="center"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
        <ImageView
            android:id="@+id/iv_remove"
            android:layout_width="@dimen/records_tag_remove_size"
            android:layout_height="@dimen/records_tag_remove_size"
            android:src="@drawable/ic_remove_circle_red_400_24dp"
            app:layout_constraintEnd_toEndOf="@id/tv_name"
            app:layout_constraintTop_toTopOf="@id/tv_name"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>